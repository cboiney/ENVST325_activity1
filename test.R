print("test")

prontly <- 5

print(prontly)

prontly <- prontly*prontly

prontly = 74

func = c(prontly, prontly+100)
names = c("one", "prontly2")

mean_v = mean(func)

#style for subsetting vectors
#peaks[1]

#dataframes
frame <- data.frame(elev = func, 
           name = names)
